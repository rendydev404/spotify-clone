
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rendydev.spotifyclone',
  appName: 'Spotify Clone',
  webDir: 'out', // UBAH BARIS INI
};

export default config;